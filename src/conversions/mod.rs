/*!
 * Conversions module for main TypeScript to IR transformations
 *
 * This module contains implementations of the main conversion traits:
 * - `ir`: Implementation of `IRFromTypeScript` for converting TypeScript code to IR
 */

pub mod ir;
